//Template Literal — backtick (`)
const name = "ฟ้า";
//const old1 = "สวัสดี " + name + "!";
const new1 = `สวัสดี ${name}!`;   // ดีกว่า

// Arrow Function — ย่อ 3 ขั้น
function add(a, b) {
  return a + b;
}

const add2 = (a, b) => {
  return a + b;
};

const add3 = (a, b) => a + b;   // สั้นสุด
